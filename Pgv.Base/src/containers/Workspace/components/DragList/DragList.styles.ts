import styled from "pgv-lib/ui/emotion/styled"
import { Box as BoxMUI } from "pgv-lib/ui/material"

export const BoxStyled = styled(BoxMUI)`
  display: flex;
  flex-direction: column;
  row-gap: 16px;

  > div {
  }
`
